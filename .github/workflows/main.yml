name: Ollama Gemma 4 E4B Tunnel

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'How long to keep runner alive (hours)'
        required: true
        default: '4'
        type: choice
        options:
          - '2'
          - '4'
          - '6'

jobs:
  ollama-tunnel:
    runs-on: ubuntu-latest
    timeout-minutes: 420
    
    steps:
      - name: Install Ollama
        run: |
          curl -fsSL https://ollama.com/install.sh | sh
      
      - name: Start Ollama Server
        run: |
          export OLLAMA_HOST=0.0.0.0
          export OLLAMA_ORIGINS="*"
          export OLLAMA_NUM_PARALLEL=4
          export OLLAMA_MAX_LOADED_MODELS=2
          
          nohup ollama serve > ollama.log 2>&1 &
          
          for i in {1..30}; do
            if curl -s http://localhost:11434/api/tags > /dev/null 2>&1; then
              echo "Ollama server is ready"
              break
            fi
            sleep 2
          done
      
      - name: Pull and Run Gemma 4 E4B
        run: |
          ollama run gemma4:e4b
      
      - name: Create GPT-4 Alias
        run: |
          ollama cp gemma4:e4b gpt-4o
          ollama cp gemma4:e4b codestral
      
      - name: Install cloudflared
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          sudo mv cloudflared /usr/local/bin/
      
      - name: Create Cloudflare Tunnel
        id: tunnel
        run: |
          cloudflared tunnel --url http://localhost:11434 \
            --http-host-header localhost:11434 \
            --logfile cloudflared.log &
          
          sleep 15
          
          TUNNEL_URL=$(grep -o 'https://[a-zA-Z0-9.-]*\.trycloudflare\.com' cloudflared.log | head -1)
          
          echo "TUNNEL_URL=$TUNNEL_URL" >> $GITHUB_ENV
          echo "tunnel_url=$TUNNEL_URL" >> $GITHUB_OUTPUT
          
          echo ""
          echo "=========================================="
          echo "TUNNEL ACTIVE: $TUNNEL_URL"
          echo "=========================================="
          echo ""
          echo "VS CODE CONTINUE CONFIG:"
          echo ""
          echo '{
            "models": [
              {
                "title": "Gemma 4 E4B",
                "provider": "openai",
                "model": "gpt-4o",
                "apiBase": "'$TUNNEL_URL'/v1",
                "apiKey": "ollama",
                "capabilities": ["tool_use"],
                "supportsTools": true,
                "requestOptions": {
                  "headers": {
                    "Content-Type": "application/json",
                    "Accept": "application/json"
                  }
                }
              }
            ],
            "tabAutocompleteModel": {
              "title": "Code Completion",
              "provider": "openai",
              "model": "codestral",
              "apiBase": "'$TUNNEL_URL'/v1",
              "apiKey": "ollama"
            }
          }'
          echo ""
          echo "=========================================="
      
      - name: Keep Runner Alive
        run: |
          echo "Tunnel active for ${{ github.event.inputs.duration }} hours"
          echo "Connect via: ${{ steps.tunnel.outputs.tunnel_url }}"
          sleep ${{ github.event.inputs.duration }}h
